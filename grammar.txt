expr    : term ((PLUS|MINUS) term)*

term    : factor ((MUL|DIV) factor)*
        : factor (FACTORIAL)*

factor  : (PLUS|MINUS) factor
        : power

power   : atom (POW factor)*

atom    : INT|FLOAT
        : LPAREN expr RPAREN



FUNCTIONS

define void functionName(inputs){
    "code"
}

define full functionName(inputs){
    "code"
    output something
}
